
export function init() {
  return (dispatch, getState) => {
    // this function is dispatched once in root component
    // when it is first mounted, you can fetch stuff that is
    // needed to initialize your app here
  }
}